{
    //
    
    const sumArray = (numbers: number[]): number => {
        let sum: number = 0;

        numbers.forEach((num: number) => {
            sum += num;
        })

        return sum
    }

    //
}